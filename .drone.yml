pipeline:
  setup:
    image: node:latest
    commands: 
      - npm install
      - npm run build 

  deploy:
    image: docker/compose
    environment:
      - DOCKER_HOST=tcp://159.203.105.174:2375
    commands:
      - docker-compose up -d
    volume:
      - /var/run/docker.sock:/var/run/docker.sock