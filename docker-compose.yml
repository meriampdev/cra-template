version: '3.2'

services:
  webserver:
    build: .
    container_name: node_cra
    tty: true
    ports:
      - "8082:8082"
    commands: ["pm2-runtime start node_modules/react-scripts/scripts/start.js --name 'my-app'"]
    volumes:
      - ./:/app/:cached
